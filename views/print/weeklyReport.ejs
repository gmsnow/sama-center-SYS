<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>التقرير الاسبوعي</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f5f5f5;
            direction: rtl;
        }
        
        .container {
            background-color: white;
            border: 2px solid #333;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 20px;
            border-bottom: 2px solid #333;
        }
        
        .header-left {
            text-align: left;
            font-weight: bold;
            color: #1e3a8a;
            font-size: 16px;
        }
        
        .header-center {
            text-align: center;
        }
        
       
        
        .header-right {
            text-align: right;
            font-weight: bold;
            color: #1e3a8a;
            font-size: 16px;
        }
        
        .title-section {
            background: linear-gradient(90deg, #a7f3d0, #34d399);
            border: 3px solid #10b981;
            color: #065f46;
            text-align: center;
            padding: 20px;
            font-size: 18px;
            font-weight: bold;
            margin: 20px;
            border-radius: 10px;
        }
        
        .main-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 14px;
            margin: 20px 0;
        }
        
        .main-table th {
            border: 2px solid #333;
            padding: 12px;
            text-align: center;
            font-weight: bold;
            font-size: 16px;
        }
        
        .main-table td {
            border: 2px solid #333;
            padding: 12px;
            text-align: center;
            font-size: 14px;
        }
        
        /* Header row colors */
        .header-row th:nth-child(1) {
            background-color: #bfdbfe;
            color: #1e40af;
        }
        
        .header-row th:nth-child(2) {
            background-color: #bfdbfe;
            color: #1e40af;
        }
        
        .header-row th:nth-child(3) {
            background-color: #fef3c7;
            color: #92400e;
        }
        
        .header-row th:nth-child(4) {
            background-color: #bbf7d0;
            color: #166534;
        }
        
        .header-row th:nth-child(5) {
            background-color: #e0f2fe;
            color: #0c4a6e;
        }
        
        .header-row th:nth-child(6) {
            background-color: #e0f2fe;
            color: #0c4a6e;
        }
        
        /* Row number column */
        .row-number {
            background-color: #bfdbfe;
            color: #1e40af;
            font-weight: bold;
            width: 50px;
        }
        
        /* Day column */
        .day-column {
            background-color: #bfdbfe;
            color: #1e40af;
            font-weight: bold;
            width: 100px;
        }
        
        /* Date column */
        .date-column {
            color: #ea580c;
            font-weight: bold;
            width: 120px;
        }
        
        /* Income column */
        .income-column {
            background-color: #fef3c7;
            width: 120px;
        }
        
        /* Expense column */
        .expense-column {
            background-color: #bbf7d0;
            width: 120px;
        }
        
        /* Notes column */
        .notes-column {
            background-color: #e0f2fe;
            width: 200px;
        }
        
        /* Total rows */
        .total-row-1 {
            background-color: #f3e8ff;
        }
        
        .total-row-1 td:nth-child(2) {
            font-weight: bold;
            color: #7c3aed;
        }
        
        .total-row-1 td:nth-child(3) {
            background-color: #fbbf24;
            color: #92400e;
            font-weight: bold;
            font-size: 18px;
        }
        
        .total-row-1 td:nth-child(4) {
            background-color: #c084fc;
            color: #581c87;
            font-weight: bold;
            font-size: 18px;
        }
        
        .total-row-2 {
            background-color: #fef3c7;
        }
        
        .total-row-2 td:nth-child(2) {
            font-weight: bold;
            color: #92400e;
        }
        
        .total-row-2 td:nth-child(3) {
            background-color: #bbf7d0;
            color: #166534;
            font-weight: bold;
            font-size: 18px;
        }
        
        .footer {
            display: flex;
            justify-content: space-between;
            padding: 15px 20px;
            background-color: #f9fafb;
            border-top: 2px solid #333;
            font-weight: bold;
        }
        
        .footer-left {
            color: #374151;
        }
        
        .footer-right {
            color: #374151;
        }
        @media print { * { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; } }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="header-left" style="
                text-align: left; 
                font-weight: 600; 
                font-size: 14px;
                line-height: 1.5;
            ">
                SAMA CENTER FOR<br>
                PHYSIOTHERAPY & REHABILITATION
            </div>
        
            <div class="header-center">
                <div class="logo" style="
                    width: 65px; 
                    height: 65px; 
                    border-radius: 50%; 
                    margin-left: 60px;
                    overflow: hidden; 
                    border: 3px solid white;
                    box-shadow: 0 2px 8px rgba(0,0,0,0.25);
                ">
                    <img src="/assets/img/logos/center.png" alt="Logo" style="width: 100%; height: 100%; object-fit: cover;">
                </div>
            </div>
        
            <div class="header-right" style="
                text-align: right; 
                font-weight: 600; 
                font-size: 16px;
                line-height: 1.5;
            ">
                مركز سما للعلاج الطبيعي<br>وإعادة التأهيل
            </div>
        </div>
        
        
        <!-- Title Section -->
        <div class="title-section" id="title-section">
            التقرير الأسبوعي (الوارد والصادر اليومي للفترتين)<br>
            <span id="week-dates"></span>
        </div>
        
        
        <!-- Main Table -->
        <table class="main-table">
            <thead>
                <tr class="header-row">
                    <th>م</th>
                    <th>اليوم</th>
                    <th>التاريخ</th>
                    <th>الوارد</th>
                    <th>الصادر</th>
                    <th>ملاحظات</th>
                </tr>
            </thead>
            <tbody>
                <% weekData.forEach((d, i) => { %>
                  <tr>
                    <td class="row-number"><%= i + 1 %></td>
                    <td class="day-column"><%= d.day %></td>
                    <td class="date-column"><%= d.date %></td>
                    <td class="income-column"><%= d.income %></td>
                    <td class="expense-column"><%= d.expense %></td>
                    <td class="notes-column"></td>
                  </tr>
                <% }) %>
                <tr class="total-row-1">
                  <td></td>
                  <td>الإجمالي الأسبوعي</td>
                  <td></td>
                  <td class="income-column">
                    <%= weekData.reduce((sum, d) => sum + (d.income || 0), 0) %>
                  </td>
                  <td class="expense-column">
                    <%= weekData.reduce((sum, d) => sum + (d.expense || 0), 0) %>
                  </td>
                  <td></td>
                </tr>
              </tbody>
        </table>
        
        <!-- Footer -->
        <div class="footer">
            <div class="footer-left">إدارة المركز <br>————</div>
            <div class="footer-right">السكرتارية <br>————</div>
        </div>
    </div>
    <script>
       const today = new Date();

// Calculate previous Saturday
const day = today.getDay(); // 0 = Sunday, 6 = Saturday
const diffToSat = (day === 6 ? 0 : day + 1);
const saturday = new Date(today);
saturday.setDate(today.getDate() - diffToSat);

// Calculate next Friday
const friday = new Date(saturday);
friday.setDate(saturday.getDate() + 6);

const options = { day: 'numeric', month: 'long', year: 'numeric' };
document.getElementById('week-dates').textContent =
    `من تاريخ ${saturday.toLocaleDateString('ar-EG', options)} - إلى ${friday.toLocaleDateString('ar-EG', options)}`;
    </script>
    <script>
        window.onload = function() {
          window.print();
        };
      </script>
</body>
</html>
